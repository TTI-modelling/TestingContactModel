{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Node tracing adherence attribute schema",
  "description": "A schema for validating the tracing adherence attributes of nodes in a household branching process model",
  "type": "object",
  "anyOf": [
    {"required": ["will_uptake_isolation"]},
    {"required": ["propensity_imperfect_isolation"]}
  ],
  "properties": {
    "will_uptake_isolation": {
      "description": "Will this node uptake isolation?",
      "type": "boolean"
    },
    "propensity_imperfect_isolation": {
      "description": "The propensity of this node to perform imperfect isolation?",
      "type": ["boolean", "null"]
    }
  }
}